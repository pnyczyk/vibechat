{
  "servers": [
    {
      "id": "codex-tasks",
      "command": "codex-tasks",
      "args": ["mcp"],
      "description": "Codex Tasks workflow",
      "enabled": true
    },
    {
      "id": "filesystem",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "."],
      "description": "Filesystem access",
      "enabled": true
    }
  ]
}
